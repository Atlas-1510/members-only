<!DOCTYPE html>
<html>
  <%- include('./partials/head.ejs') %>
  <body>
    <h1>Home Page</h1>
    <%- include("./partials/messages.ejs") %> <% if (!locals.currentUser) { %>
    <a href="/signup">Sign Up</a>
    <a href="/login">Log In</a>
    <% } else { %>
    <a href="/article/create">Create Article</a>
    <a href="/logout">Log Out</a>
    <% if (locals.currentUser.isMember) { %>
    <a href="/member/revokeMembership">Revoke Membership</a>
    <% } else { %>
    <a href="/member/signup">Become a member </a>
    <% } %> <% } %> <%- include("./partials/articles.ejs", {articles:
    locals.articles}) %>
  </body>
</html>
