<!DOCTYPE html>
<html>
  <%- include('./partials/head.ejs') %>
  <body>
    <h1>Members Only</h1>
    <% if (locals.user) { %>
    <h1>Hello <%= user.fullName %>, you are currently logged in.</h1>
    <% } else { %>
    <a href="/signup">Sign Up</a>
    <% } %>
  </body>
</html>
